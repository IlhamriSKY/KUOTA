# ===========================================
# KUOTA - Configuration
# ===========================================
# Copy this file to .env and fill in values
# cp .env.example .env

# Server port (default: 3000)
PORT=3000

# Environment (development | production)
# NODE_ENV=production

# GitHub OAuth App Client ID (optional — for device flow login)
# Create at: https://github.com/settings/developers
# Enable "Device Flow" in the OAuth App settings
# GITHUB_OAUTH_CLIENT_ID=

# Auto-refresh interval in minutes (default: 60)
AUTO_REFRESH_MINUTES=60

# Encryption secret (auto-generated if not set)
# WARNING: If you change this after storing tokens, all encrypted data becomes unreadable
# APP_SECRET=

# MySQL backup config (optional — can also be set via Settings page)
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=
# MYSQL_DATABASE=copilot_quota
